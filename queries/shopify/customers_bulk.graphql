query CustomersBulk($query: String) {
  customers(query: $query, sortKey: UPDATED_AT) { edges { node {
    id createdAt updatedAt numberOfOrders
    amountSpent { amount currencyCode }
    defaultEmailAddress { emailAddress }
  } } }
}
