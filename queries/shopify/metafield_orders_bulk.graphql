query MetafieldOrdersBulk ($query: String) {
  orders(query: $query) {
    edges { node {
      id
      metafields {
        edges { node { id namespace key value type description createdAt updatedAt } }
      }
    } }
  }
}
