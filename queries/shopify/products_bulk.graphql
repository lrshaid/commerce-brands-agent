query ProductsBulk($query: String) {
  products(query: $query, sortKey: UPDATED_AT) { edges { node {
    id title productType vendor createdAt updatedAt
    variants { edges { node {
      id sku price inventoryQuantity inventoryItem { id }
    } } }
  } } }
}
