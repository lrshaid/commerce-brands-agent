query MetafieldProductVariantsBulk ($query: String) {
  productVariants(query: $query) {
    edges { node {
      id
      metafields {
        edges { node { id namespace key value type description createdAt updatedAt } }
      }
    } }
  }
}
