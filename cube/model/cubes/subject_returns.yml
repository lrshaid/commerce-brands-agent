# GENERATED by scripts/generate_cube_model.py from semantic/serving_contract.yaml
# DO NOT EDIT BY HAND — edit the serving contract and re-run the generator.
cubes:
- name: subject_returns
  public: false
  sql_table: analytics.fct_returns
  description: Refund/return reconciliation at original order-line grain. Refund values
    take precedence. Includes cancellations; not the same exclusion scope as revenue
    RMV. Recognition is latest refund timestamp per line, not individual refund event
    history. Pending return-only rows have null recognition dates.
  dimensions:
  - name: shop_key
    sql: shop_key
    type: string
    public: false
  - name: match_status
    sql: match_status
    type: string
    public: true
  - name: rmv_recognition_ts_utc
    sql: rmv_recognition_ts_utc
    type: time
    public: true
  measures:
  - name: line_count
    sql: order_line_item_id
    type: count_distinct
    description: Distinct original order lines in the selected shop.
  - name: rmv
    sql: rmv_merchandise_amount
    type: sum
    description: Stored-negative merchandise value; cancellations are not excluded
      in this fact.
  - name: return_units
    sql: returned_quantity
    type: sum
    description: Stored-negative quantities. Includes pending returns unless recognition
      date is filtered.
  - name: refund_tax
    sql: rmv_tax_amount
    type: sum
    description: Stored-negative tax amounts.
  - name: allocated_adjustments
    sql: allocated_adjustment_amount
    type: sum
    description: Order adjustments allocated across lines; may be attributed across
      recognition dates.
