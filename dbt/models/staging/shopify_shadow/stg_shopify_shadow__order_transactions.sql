{{ config(materialized='view', contract={'enforced': true}) }}
select
    shop_key,
    transaction_gid,
    order_gid,
    kind,
    status,
    amount,
    currency_code,
    presentment_amount,
    presentment_currency_code,
    payment_id,
    parent_transaction_gid,
    gateway,
    formatted_gateway,
    created_at,
    processed_at,
    is_test,
    error_code,
    receipt,
    settlement_currency,
    settlement_currency_rate,
    multi_capturable,
    manually_capturable,
    maximum_refundable,
    location_gid,
    location_name,
    device_gid,
    payment_details,
    fees,
    acquirer_reference_number,
    original_payload,
    source_extraction_id,
    source_updated_at,
    source_published_at,
    extracted_at
from {{ source('shopify_entities_shadow', 'order_transactions') }}
