{{ config(materialized='view', contract={'enforced': true}) }}
select
    shop_key,
    refund_line_item_gid,
    refund_gid,
    order_gid,
    order_line_item_gid,
    quantity,
    restock_type,
    restocked,
    location_gid,
    location_name,
    line_item,
    price_amount,
    price_currency_code,
    presentment_price_amount,
    presentment_price_currency_code,
    subtotal_amount,
    subtotal_currency_code,
    presentment_subtotal_amount,
    presentment_subtotal_currency_code,
    total_tax_amount,
    total_tax_currency_code,
    presentment_total_tax_amount,
    presentment_total_tax_currency_code,
    original_payload,
    source_extraction_id,
    source_updated_at,
    source_published_at,
    extracted_at
from {{ source('shopify_entities_shadow', 'refund_line_items') }}
