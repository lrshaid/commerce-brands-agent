{{ config(materialized='view', contract={'enforced': true}) }}
select
    shop_key,
    transaction_gid,
    refund_gid,
    order_gid,
    kind,
    status,
    amount,
    currency_code,
    presentment_amount,
    presentment_currency_code,
    payment_id,
    parent_transaction_gid,
    gateway,
    formatted_gateway,
    created_at,
    processed_at,
    is_test,
    error_code,
    receipt,
    payment_details,
    fees,
    original_payload,
    source_extraction_id,
    source_updated_at,
    source_published_at,
    extracted_at
from {{ source('shopify_entities_shadow', 'refund_transactions') }}
