version: 2
sources:
  - name: klaviyo_api
    database: "{{ env_var('GOOGLE_CLOUD_PROJECT', 'commerce-agents-dev') }}"
    schema: raw_klaviyo
    tables:
      - name: events
      - name: campaigns
      - name: ingestion_runs
